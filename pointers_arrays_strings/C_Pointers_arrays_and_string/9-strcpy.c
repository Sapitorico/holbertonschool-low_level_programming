#include "main.h"
/**
 * _strcpy - copies to string
 *
 * @dest: buffer pointed dest
 * @src: string pointed
 * Return: pointer to new copy stirng
 */
char *_strcpy(char *dest, char *src)
{
    char *copy = dest;


    while (*copy++ = *src++)
        ;
    return (dest);
}
