#include "main.h"
/**
 * _puts - function that prints a string, followed by a new line
 *
 *@:str: is a pinter to a arrway
 */
void _puts(char *str)
{
	int puts;

	for (puts = 0 ; str[puts] '\0'; ++puts)
	{
		_putchar (str[puts]);
	}
	_putchar ('\n')
}
