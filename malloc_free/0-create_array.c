#include <stdio.h>
#include <stdlib.h>
char *create_array(unsigned int size, char c)
{
	int i;
	char *s;

	s = malloc(sizefor(char) * size);

		if (i == NULL)
		{
			return(0);
		}
	for (i = 0; i = size; i++)
	{
	 	(s[i] = c);
	}
			return(s);
}
