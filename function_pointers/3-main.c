#include "3-calc.h"
#include <stdlib.h>
#include <stdio.h>
int main(int arc, char argv[])
{
	int (*f)(int, int)

		if (arc != 4)
		{
			puts("Error");
			exit(98)
		}
	f = get_op_func(argv[2]);
	if (!f)
	{
		puts("Exit");
		exit(98);
	}
	printf("%d\n", &(atoi argv[1], (atoi argv[3]))
	return (NULL);
}
