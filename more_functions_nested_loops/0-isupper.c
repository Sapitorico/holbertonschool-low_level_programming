#include "main.h"
/**
 * _isupper - check if ch is uppercase
 *
 *@c: (ch) integer to check
 *
 * return: 1 if c _isupper, 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	return (1);
	return (0);
}
