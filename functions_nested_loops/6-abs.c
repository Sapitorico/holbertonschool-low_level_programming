#include "main.h"
/**
 * _abs - compute absolute value of number
 *
 * @number: number
 * Return: absolute value of number
 */
int _abs(int number)
{
    return number < 0 ? -number : number;
}
